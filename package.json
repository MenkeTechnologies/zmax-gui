{
  "name": "zemacs-gui",
  "version": "0.1.0",
  "private": true,
  "description": "zemacs-gui — desktop GUI wrapper around the zemacs editor (Helix fork). Runs the zemacs binary in an embedded PTY terminal with the shared zgui-core baseline.",
  "scripts": {
    "dev": "pnpm install && node scripts/copy-embed-terminal.mjs && tauri dev",
    "build": "pnpm install && node scripts/copy-embed-terminal.mjs && tauri build",
    "tauri": "tauri",
    "test": "bash scripts/test.sh",
    "test:js": "node --test crates/zemacs-gui-core/webui/menu.test.cjs",
    "test:rust": "cargo test --manifest-path app/src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
