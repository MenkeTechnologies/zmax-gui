{
  "name": "zemacs-gui",
  "version": "0.1.0",
  "private": true,
  "description": "zemacs-gui — desktop GUI wrapper around the zemacs editor (Helix fork). Runs the zemacs binary in an embedded PTY terminal with the shared zgui-core baseline.",
  "scripts": {
    "dev": "node scripts/copy-embed-terminal.mjs && tauri dev",
    "build": "node scripts/copy-embed-terminal.mjs && tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
