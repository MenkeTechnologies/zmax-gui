{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the zemacs-gui window. The embedded terminal commands (terminal_spawn/write/resize/kill) are app-local Tauri commands; only core defaults are needed.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
